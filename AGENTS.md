# 후임자와 함께 일하는 AI 지침

- README → docs/BRIEF → docs/STATUS 순서로 읽고 업무에 필요한 OPERATIONS/SOURCES만 추가로 읽는다.
- 모든 사실에 기준일과 근거를 붙인다. 계획·초안·승인·실행·검증 완료를 구분한다. 날짜가 지났다고 완료 처리하지 않는다.
- 이 저장소는 로컬 기록의 증류본이다. 2026-09-22 현재 운영 상태를 검증한 것으로 말하지 않는다.
- 최근 작성 문서라도 초안이면 승인 사실을 대신하지 못한다. 충돌 시 동일 항목의 승인·실행 증빙과 해당 시점 자료를 대조한다. 해결되지 않으면 양쪽 근거와 확인 담당자를 기록한다.
- 참가자 수, 양일 출석 수, 이수자 수, 설문 응답 수를 섞지 않는다. 성과·달성률은 원자료와 산식을 확인한 뒤 계산한다.
- 작업 결과는 초안과 확인 질문까지 구체적으로 만든다. 기존 기록상 대외 발신·예약 확정은 운영 총괄 컨펌 대상이다. 후임자에게 발송 권한이 위임되었는지 확인한다. 명시적 발송 지시 없이 Slack·문자·메일·카톡을 보내지 않는다.
- Computer Use는 최후 수단이다. API, MCP/앱 커넥터, CLI, 브라우저 자동화를 먼저 확인한다. 여러 방법이 가능하면 사용자에게 먼저 물어본다.
- 개인정보·비밀키·참가자 응답·발송 로그를 Git에 넣지 않는다. 권한이 필요한 원자료는 기존 보관처에서 읽고 필요한 비식별 요약만 남긴다.
- 구형 운영 스크립트는 import나 --help만으로도 안전하다고 가정하지 않는다. 먼저 코드를 읽고 하드코딩된 기수·명단·열 번호·외부 변경·dry-run 동작을 확인한다.
- 변경 시 docs/STATUS.md의 현재 상태와 docs/DECISIONS.md의 추가 이력을 함께 갱신한다. 이전 결정을 지우지 말고 대체 근거를 남긴다.
- 외부 공유 파일은 Drive 사업 폴더 내 해당 업무 하위 폴더에 둔다. Git은 맥락·절차·결정의 요약을 관리한다.
- 모르는 연락처·계정·기한·승인을 추측하지 않는다. 검증 실패와 접근 불가를 명시한다.

- 이 저장소는 공개 저장소다. 내부 문서 링크·문서 ID·실명 담당표·사업비·참여 실적·만족도 수치는 비공개 부록에 기록한다. 새 자료를 요약할 때에도 이 공개 경계를 유지한다.
